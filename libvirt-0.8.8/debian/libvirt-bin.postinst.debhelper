# Automatically added by dh_installinit
if [ -e "/etc/init/libvirt-bin.conf" ]; then
	invoke-rc.d libvirt-bin start || exit $?
fi
# End automatically added section
# Automatically added by dh_pycentral
rm -f /var/lib/pycentral/libvirt-bin.pkgremove
if which pycentral >/dev/null 2>&1; then
	pycentral pkginstall libvirt-bin
	if grep -qs '^libvirt-bin$' /var/lib/pycentral/delayed-pkgs; then
		sed -i '/^libvirt-bin$/d' /var/lib/pycentral/delayed-pkgs
	fi
fi
# End automatically added section
